<div class="col-sm-6 col-md-4 col-lg-4 product-length">
  <%= image_tag(product.image_url, class: "img-responsive") %>
  <h3><strong><%= product.name %></strong></h3>
  <p><%= product.description %></p>
  <p><%= product.color %></p>
  <p><strong><%= product.price %> €</strong></p>
  <% if signed_in? && current_user.admin? %>
    <p><%= link_to 'Show', product %> |<%= link_to 'Edit', edit_product_path(product) %> | <%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %>
  	</p>
  <% else %>
  <br>
    <%= link_to product, class: "btn btn-default" do %> 
      <i class="glyphicon glyphicon-eye-open"></i> Details
    <% end %>
  <% end %>
</div><!-- /col -->