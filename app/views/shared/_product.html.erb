
      <div class="col-xs-12 col-sm-6 col-md-3 product-overview">
        <h1><%product.name %></h1>
        <%= image_tag(product.image_url, class:'img-responsive') %>
        <p> Price: &euro;<%= product.price %></p>
        <p> Color: <%= product.color %></p>
        <p><%= truncate(product.description) %></p>
        <br>
      </div>
      
     
    

   